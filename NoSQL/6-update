//Update a document
db.getCollection('school').updateMany(
  { name: "Holberton school" },  // Filter by name
  { $set: { address: "972 Mission street" } },  // Set the new address
  { upsert: false }  // Only update existing documents, don't insert
);